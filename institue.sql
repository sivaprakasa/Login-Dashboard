create database institue;

use institue;

create table user(id int primary key auto_increment, username varchar(20), password varchar(6), cpassword varchar(6), email varchar(30), phone varchar(30), dob date);